apiVersion: v1
kind: Service
metadata:
  name: auth-service-cassandradb
  labels:
    app: auth-service
    tier: db
spec:
  ports:
    - port: 9042
      targetPort: 9042
  selector:
    app: auth-service
    tier: db
